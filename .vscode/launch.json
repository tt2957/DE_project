{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Steam Collector",
      "type": "python",
      "request": "launch",
      "module": "src.ingest.collect",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
